server:
  port: 7070

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_boot_with_jpa
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: EMPLOYEE-DATABASE
